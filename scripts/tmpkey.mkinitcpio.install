#!/bin/bash

build() {
    add_runscript
}

help() {
    cat <<HELPEOF
This hook enables the storage of a LUKS key within
the boot image by extracting the key at boot time
to a tmpfs. 
HELPEOF
}

# vim: set ft=sh ts=4 sw=4 et:
